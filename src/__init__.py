"""
src package initializer.

This file makes the `src` directory a Python package so that modules inside
(e.g., utils, train, batch_predict) can be imported in tests and other scripts.

Example:
    from src.utils import add_numbers
"""
