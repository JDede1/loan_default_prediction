# requirements.airflow.txt
# Airflow providers needed for this project

# Google Cloud integration (GCS, BigQuery, etc.)
apache-airflow-providers-google>=10.0.0

# Postgres support (Airflow backend + hooks/operators)
apache-airflow-providers-postgres>=5.7.0

# Vertex AI SDK for submitting training jobs
google-cloud-aiplatform>=1.38.0
