# requirements.airflow.txt
# === Airflow-specific extras for DAG tasks ===

# MLflow client (logging, model registry integration)
mlflow==3.1.4

# Google Cloud Storage support (used in your scripts & monitoring)
google-cloud-storage
gcsfs

# Config & utility helpers
python-dotenv
pyyaml
requests
