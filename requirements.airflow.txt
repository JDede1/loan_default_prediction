# requirements.airflow.txt
# Airflow extras - keep minimal, let constraints handle versions

# MLflow for experiment tracking + registry
mlflow==3.1.4

# Utilities
python-dotenv
pyyaml
requests   # unpinned - Airflow constraints will pick a safe version

# Google Cloud support
google-cloud-storage
gcsfs

