# MLflow aligned with tracking server
mlflow==3.1.4

# Core model/runtime deps (let pip resolve compatible versions)
numpy
pandas
pyarrow
scikit-learn
scipy
xgboost

# Serving API deps
fastapi
uvicorn[standard]

# GCS support
google-cloud-storage
gcsfs
