# Requirements.txt

# === Core ML libs (Airflow constraints handle versions) ===
numpy
pandas
scikit-learn
matplotlib
joblib

# === Specific ML libraries ===
xgboost==1.7.6

# === MLflow ===
mlflow==3.1.4

# === Utilities ===
python-dotenv        # let Airflow constraints choose (currently 1.0.0)
requests==2.31.0
pyyaml==6.0.1
psycopg2-binary==2.9.9

# === Evidently (compatible with Pydantic v2) ===
evidently>=0.4.0

# === Phase 2 Additions: Google Cloud integration ===
google-cloud-storage  # let constraints decide (currently 2.14.0)
gcsfs                 # let constraints decide (currently 2023.12.2.post1)
fsspec>=2023.6.0      # required for Pandas + gcsfs
